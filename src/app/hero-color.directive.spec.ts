import { HeroColorDirective } from './hero-color.directive';

describe('HeroColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroColorDirective();
    expect(directive).toBeTruthy();
  });
});
